# ── Dependencies ──
node_modules/

# ── Build output ──
dist/
build/
target/

# ── Environment / secrets ──
.env
.env.*
!.env.example

# ── Tauri ──
src-tauri/gen/schemas/

# ── Local reference material (screenshots, binaries, format docs) ──
references/

# ── GSD (agent workspace) ──
.gsd

# ── Background shell state ──
.bg-shell/

# ── Editor / OS ──
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# ── Logs ──
*.log

# ── Game installs (local testing — each ~35-44GB) ──
The Secret World/
our_downloader_secretworld/
working_secretworld/
ours-recent/
ours-loggedin/
normal-downloaded/
normal-first-launch/
normal-full-patch/
normal-full-loggedin/
installer-finished/
failed-attempt/
weird-size/

# ── NSIS installer extracted files (from Funcom installer analysis) ──
*{app}*
*{tmp}*

# ── Game binaries / Ghidra projects (reverse engineering) ──
TheSecretWorldInstaller.exe
*.gpr
*.rep/

# ── Research scripts (one-off analysis, not part of the app) ──
*.py
forensic_results.txt
interceptor_output.txt

# ── Generated data files ──
le.idx.gz
*.json
!package.json
!package-lock.json
!tsconfig.json
!tsconfig.node.json
!src-tauri/tauri.conf.json

# ── Misc ──
coverage/
.cache/
tmp/
test_full_download/
bench_staging/
*.bak

# ── Debug tools (not part of the app) ──
src-tauri/tools/

# ── GSD baseline (auto-generated) ──
.next/
__pycache__/
*.pyc
.venv/
venv/
vendor/
