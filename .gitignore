# ── Dependencies ──
node_modules/

# ── Build output ──
dist/
build/
target/

# ── Environment / secrets ──
.env
.env.*
!.env.example

# ── Tauri ──
src-tauri/gen/schemas/

# ── Local reference material ──
references/

# ── GSD (agent workspace) ──
.gsd

# ── Background shell state ──
.bg-shell/

# ── Editor / OS ──
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# ── Logs ──
*.log

# ── Game install (local testing) ──
The Secret World/
our_downloader_secretworld/
working_secretworld/

# ── Misc ──
coverage/
.cache/
tmp/

# Test/research artifacts
test_full_download/
bench_staging/
*.json
!package.json
!package-lock.json
!tsconfig.json
!tsconfig.node.json
!src-tauri/tauri.conf.json
interceptor_output.txt
*.bak

# ── Debug tools (not part of the app) ──
src-tauri/tools/

# ── GSD baseline (auto-generated) ──
.next/
__pycache__/
*.pyc
.venv/
venv/
vendor/
