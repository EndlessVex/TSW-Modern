# ── Dependencies ──
node_modules/

# ── Build output ──
dist/
build/
target/

# ── Environment / secrets ──
.env
.env.*
!.env.example

# ── Tauri ──
src-tauri/gen/schemas/

# ── Local reference material (screenshots, binaries, format docs) ──
references/

# ── GSD (agent workspace) ──
.gsd

# ── Git worktrees (isolated feature branches) ──
.worktrees/

# ── Background shell state ──
.bg-shell/

# ── Editor / OS ──
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# ── Logs ──
*.log

# ── Game installs (local testing — each ~35-44GB) ──
game-installs/
The Secret World/
our_downloader_secretworld/
working_secretworld/

# ── Research (scripts, Ghidra, NSIS, data) ──
research/

# ── Internal docs (local only) ──
docs/
CLAUDE.md
*.json
!package.json
!package-lock.json
!tsconfig.json
!tsconfig.node.json
!src-tauri/tauri.conf.json

# ── Release binaries (built locally) ──
Secret.World.Launcher.exe
decompress-helper.exe

# ── Misc ──
coverage/
.cache/
tmp/
test_full_download/
bench_staging/
*.bak

# ── Debug tools (not part of the app) ──
tools/
src-tauri/tools/

# ── Rustc temp directories (created by some rustup toolchains) ──
rmeta*/
rustc*/

# ── GSD baseline (auto-generated) ──
.next/
__pycache__/
*.pyc
.venv/
venv/
vendor/
